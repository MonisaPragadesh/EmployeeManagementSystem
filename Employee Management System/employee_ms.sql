CREATE DATABASE employee;

use employee;

create table empdata (
   Id INT,
    NAME VARCHAR(60),
    Email_Id VARCHAR(60),
    Phone_no BIGINT, 
    Address VARCHAR(60), 
    Post VARCHAR(60), 
    Salary int
);